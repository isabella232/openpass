configurations {
    frontend {}
}

moabDependencies {
    frontend ':identification:id-controller:id-controller-ui'
}

msbuildPackage {
    from(configurations.frontend) {
        into("Criteo.IdController/dist")
    }
}
