configurations {
    frontend {}
}

moabDependencies {
    frontend ':identification:id-controller:Criteo.IdController.UI'
}

msbuildPackage {
    from(configurations.frontend) {
        into("Criteo.IdController/dist")
    }
}
