// IdControllerWidget class must be initialized from the front-end
// example: const widget = new IdControllerWidget();

class IdControllerWidget {
    constructor() {
        console.log('IdController initialized!');
    }
}
